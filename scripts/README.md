# Scripts Directory

This directory is reserved for helper scripts and utilities.

Currently, all analyses are performed in Jupyter notebooks (`notebooks/` directory).

## Future Scripts (Planned)

### Data Processing
- `download_gdc_data.py` - Automated GDC data download
- `preprocess_rnaseq.py` - RNA-seq normalization pipeline
- `merge_clinical_data.py` - Clinical data integration

### Analysis
- `run_differential_expression.py` - Batch differential expression
- `run_gsea_batch.py` - GSEA for multiple gene sets
- `train_ml_models.py` - Batch ML model training

### Utilities
- `generate_figures.py` - Regenerate all figures
- `export_results.py` - Export tables for publication
- `validate_reproducibility.py` - Check reproducibility

---

For now, refer to Jupyter notebooks for complete analysis pipeline.
